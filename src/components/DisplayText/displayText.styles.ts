import { StyleSheet } from "react-native";

export default StyleSheet.create({
  text: {
    fontSize: 26,
    fontWeight: "500",
    letterSpacing: 1.0,
    fontStyle: "italic",
    position: "absolute",
  },
});
